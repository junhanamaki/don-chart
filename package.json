{
  "name": "don-chart",
  "version": "1.0.0",
  "description": "another jQuery chart plugin",
  "main": "index.js",
  "scripts": {
    "start": "node_modules/.bin/webpack-dev-server --progress --content-base sample --config webpack.server.config.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clear:dist": "rm -rf dist",
    "clear": "npm run clear:dist",
    "build:dist:dev": "node_modules/.bin/webpack",
    "build:dist:prod": "NODE_ENV=production node_modules/.bin/webpack",
    "build:dist": "npm run build:dist:dev & npm run build:dist:prod",
    "build": "npm run build:dist",
    "rebuild:dist": "npm run clear:dist & npm run build:dist",
    "rebuild": "npm run rebuild:dist"
  },
  "keywords": [
    "jquery",
    "jquery-plugin",
    "chart"
],
  "author": "jun.hanamaki@gmail.com",
  "license": "ISC",
  "repository": "https://github.com/junhanamaki/don-chart",
  "devDependencies": {
    "babel-istanbul-loader": "^0.1.0",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.6.0",
    "eslint": "^2.6.0",
    "eslint-config-airbnb": "^6.2.0",
    "eslint-loader": "^1.3.0",
    "eslint-plugin-react": "^4.2.3",
    "jquery": "^2.2.2",
    "webpack": "^1.12.14",
    "webpack-dev-server": "^1.14.1"
  }
}
